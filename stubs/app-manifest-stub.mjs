/**
 * Stub para o alias `#app-manifest` — satisfaz o import-analysis do Vite 7
 * quando o ramo `import.meta.server` é analisado em `nuxt/dist/app/composables/manifest.js`.
 * Manter `experimental.appManifest: false` em `nuxt.config.ts`; se voltares a activar o app manifest,
 * remove o alias `vite.resolve.alias['#app-manifest']` e este ficheiro deixa de ser usado.
 */
export default {}
